# Increasing # of Records Returned by Preview Calls

We've just deployed a change to preview calls that will allow you to return a maximum of 50 records per preview call. The previous limit was 10.

To take advantage of this, just set your records parameter to the number you want.

Note that, just like before, requesting more records per API call will use up your available downloads faster.
